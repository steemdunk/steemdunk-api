describe('Vote queue', () => {
  require('./vote_queue');
});

describe('Vote log', () => {
  require('./vote_log');
});

describe('Author', () => {
  require('./author');
});

describe('Bot support', () => {
  require('./bot_support');
});
